// 比赛节奏
export enum Tempo {
  SLOW = 'slow',
  BALANCED = 'balanced',
  FAST = 'fast',
}

// 球场宽度
export enum PitchWidth {
  NARROW = 'narrow',
  BALANCED = 'balanced',
  WIDE = 'wide',
}

// 防线高度
export enum DefensiveLine {
  LOW = 'low',
  MID = 'mid',
  HIGH = 'high',
}
