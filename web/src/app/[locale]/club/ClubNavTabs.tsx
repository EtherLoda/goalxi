// ClubNavTabs intentionally renders nothing — the global Sidebar (under
// `components/dashboard/Sidebar.tsx`) owns all primary navigation now.
// This stub is kept so that future club-specific sub-navigation can be
// re-introduced without re-deriving the layout.
export default function ClubNavTabs() {
    return null;
}