export enum ForumCategoryScope {
  PUBLIC = 'public',
}

export enum ForumReactionType {
  LIKE = 'like',
}

export enum ForumThreadSort {
  LATEST = 'latest',
  HOT = 'hot',
}

export enum ForumPostSort {
  OLDEST = 'oldest',
  NEWEST = 'newest',
}
